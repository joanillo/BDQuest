municipis
--------------
mysql> CREATE DATABASE municipis CHARACTER SET utf8 COLLATE utf8_general_ci;
mysql> GRANT ALL ON municipis.* TO bdquest@localhost;
mysql> flush privileges;

$ mysql -u root -p municipis -f < dades_municipis_mysql.sql > /home/ubuntu/control_errors.log 2>&1

sakila
---------------
mysql> CREATE DATABASE sakila CHARACTER SET utf8 COLLATE utf8_general_ci;
mysql> GRANT ALL ON sakila.* TO alumne@localhost;
mysql> GRANT ALL ON sakila.* TO bdquest@localhost;
mysql> flush privileges;

mysql> SOURCE /home/ubuntu/sakila-db/sakila-schema.sql;
mysql> SOURCE /home/ubuntu/sakila-db/sakila-data.sql;

langtrainer
----------------
En el servidor:
mysqldump -i --complete-insert -u root -p -r /home/ubuntu/copia_langtrainer210721.sql -v langtrainer

En local:
mysql> CREATE DATABASE langtrainer CHARACTER SET utf8 COLLATE utf8_general_ci;
mysql> GRANT ALL ON langtrainer.* TO alumne@localhost;
GRANT ALL ON langtrainer.* TO bdquest@localhost;
mysql> flush privileges;

$ mysql -u root -p langtrainer -f < ./copia_langtrainer210721.sql > ./control_errors.log 2>&1
